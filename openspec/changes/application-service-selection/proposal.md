# 应用服务选配

同一产品线不同系统可使用不同服务，例如 NN iOS 使用 Sentry，NN Android 使用腾讯 Bugly。将服务选择、提供商及其非敏感应用参数持久化在应用层。平台限制用于判断适配范围，不能代替用户选配。

存量 NULL 配置保持既有能力，显式空数组关闭全部可选服务，新产品线初始 iOS 应用待选配。崩溃提供商可选 Sentry、Bugly 或不启用；当前每应用一个主提供商。Bugly 和 Android Sentry 本次仅接通配置及控制台跳转，数据 API 仍待适配。
